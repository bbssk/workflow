_schema-version: '3.2'
ID: leaverequest
version: 0.0.1
modules:
  - name: leave-request
    type: com.sap.application.content
    path: leave-request
    requires:
      - name: sap_processautomation
        parameters:
          content-target: true
          service-key:
            config:
              deployUsageScenario: workflow
            name: spa-workflow-service-key
resources:
  - name: sap_processautomation
    type: org.cloudfoundry.existing-service
    parameters:
      service: sap_processautomation
      service-plan: standard
